import React from 'react';
import { LoaderContainer, LoaderElement } from './styles'

function Loader() {
    return (
        <LoaderContainer>
            <LoaderElement />
        </LoaderContainer>
    );
};

export default Loader;
